// flags.go will define all CLI flags and their help text.
//
// Expected flags include pcap path, interface, BPF, mode, database path,
// HTTP address, log level, OUI data path, offline threshold, lifecycle
// interval, flush interval, and output format.
package config
