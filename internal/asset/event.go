// event.go will define lifecycle and audit events emitted by AssetManager.
//
// Expected event types include asset created, asset seen, asset updated,
// status changed online, status changed offline, IP added, hostname changed,
// vendor enriched, and conflict detected.
package asset
