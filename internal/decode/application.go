// application.go will hold local-discovery protocol decode placeholders.
//
// Candidate protocols for the current scope are limited to asset-discovery
// traffic visible inside the LAN, such as SSDP. Generic application payload
// parsing is intentionally out of scope unless added later as optional
// enrichment.
package decode
